# Phase 2 — Documentation Alignment

## Summary

- Updated `AGENTS.md` and `docs/STRUCTURE.md` to explicitly codify the canonical surface primitives (`.ui-card`, `.ui-panel`).
- Added guidance on where to define new surface variants (only in `ui-kit.css`) and where to document them.
